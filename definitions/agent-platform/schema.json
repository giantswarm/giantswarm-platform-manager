{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/giantswarm/giantswarm-platform-manager/main/definitions/agent-platform/schema.json",
  "title": "agent-platform definition inputs",
  "description": "The input set of the agent-platform capability: the installation's record and one choice. Everything the fileset carries derives from the record (installation.*: the registry and the installation's config.yaml.patch, read, never typed), from the fleet policy (policy.yaml: which components an owning organisation's installations run, the installations with a Slack app — where the chat gateway runs — and the gateway's shape, the connector names a target's Dex registers for a hub and the Teleport cluster) and from the shared template; a person decides two things, whether the installation serves models and whether its developer portal runs the AI chat, and with what model. Annotations: x-source says where a value comes from (registry: read from the installations registry or the repositories, never typed; generated: created once and read back from the checkout; person: a choice). x-feature names the consistency feature of features.yaml. x-files names the repository file each fileset key is rendered to (<name> the installation; document the path in the file of the YAML text its keys sit in, a key with dots in it bracketed); x-readback on a person input names the file — one, or several, the first on record holding the key answering — and key its value is read back from on record (kind value, present or host). x-renders lists the key paths the input produces, prefixed by the file: configmap (agent-platform configmap-values.yaml.patch), dex-configmap (the dex-app patch), extras (extras/agent-platform/ and the MCP servers' extras), backstage:* (the portal's agent-platform Component), teleport (teleport-fleet). List indices are written [*], per-installation map keys <name>.",
  "type": "object",
  "additionalProperties": false,
  "required": ["installation"],
  "x-files": {
    "configmap": {"repository": "configs", "path": "installations/<name>/apps/agent-platform/configmap-values.yaml.patch"},
    "dex-configmap": {"repository": "configs", "path": "installations/<name>/apps/dex-app/configmap-values.yaml.patch"},
    "backstage:agent-platform/app-config": {"repository": "management-clusters", "path": "management-clusters/<name>/extras/backstage/agent-platform/app-config.yaml", "document": "data.[app-config.agent-platform.yaml]"},
    "backstage:app-config": {"repository": "management-clusters", "path": "management-clusters/<name>/extras/backstage/backstage/app-config.yaml", "document": "data.values.backstage.appConfig"}
  },
  "properties": {
    "installation": {
      "type": "object",
      "description": "The facts on record. Read from the installations registry, the installation's config.yaml.patch, the cluster App's manifest and the portals' app-configs; never typed and never in the form.",
      "additionalProperties": false,
      "required": ["name", "baseDomain", "customer", "provider", "private", "chartLine", "musterClientId", "podCertificateRequest"],
      "properties": {
        "name": {"type": "string", "minLength": 1, "x-source": "registry", "x-feature": "runtime", "x-renders": ["configmap:components.klaus-gateway.enabled", "configmap:klausGateway", "configmap:muster.muster.oauth.mcpClient.postLoginRedirectAllowlist", "extras:agent-platform/secrets/klaus-gateway-obo-keys.yaml", "extras:agent-platform/secrets/klaus-gateway-slack-credentials.yaml"], "description": "The installation's codename. Where the policy's klausGateway.installations names it, a Slack app exists for the installation and the chat gateway runs, its credentials supplied at commit; elsewhere no gateway renders."},
        "baseDomain": {"type": "string", "minLength": 1, "x-source": "registry", "x-feature": "identity", "x-renders": ["configmap:global.domain"], "description": "The installation's base domain; every hostname of the platform (muster, agentgateway, dex, kagent, avatars) derives from it."},
        "customer": {"type": "string", "minLength": 1, "x-source": "registry", "x-feature": "runtime", "x-renders": ["configmap:components.<component>.enabled"], "description": "The owning organisation as the configs repository names it; selects the policy's component list and prefixes the connector names a target's Dex registers for this hub."},
        "provider": {"type": "string", "minLength": 1, "x-source": "registry", "x-feature": "runtime", "x-renders": ["configmap:clusterManager.networkPolicy"], "description": "The infrastructure provider; selects the cluster-manager's workload-cluster API pattern."},
        "private": {"type": "boolean", "x-source": "registry", "x-feature": "tool-access", "x-renders": ["configmap:muster.muster.oauth.server.allowPrivateIPClientMetadata", "configmap:muster.muster.oauth.server.allowPrivateIPRedirectURIs", "extras:mcp-<server>/user-values.yaml", "extras:mcp-<server>/kustomization.yaml"], "description": "Reached through Teleport: a portal on record reaches the installation's Kubernetes API through the tunnel on the portal's host (kubernetes-<name>.agent-platform.svc.cluster.local) because the installation's ingress sits on private addresses. Its MCP servers then reach Dex on private addresses, muster accepts private client metadata and redirect URIs, and a hub brokers for it through the tunnel."},
        "chartLine": {"type": "string", "enum": ["3", "4"], "x-source": "registry", "x-feature": "runtime", "x-renders": ["extras:agent-platform/kustomization.yaml patches[*]", "configmap:agent-manager.oauth", "configmap:cluster-manager.oauth"], "description": "The meta chart line the installation runs (agentPlatform.kagentApiV2 in config.yaml.patch selects 4): the 4 line pins the chart range, derives the managers' OAuth from the template's identity block and carries the serving slice."},
        "musterClientId": {"type": "string", "minLength": 1, "x-source": "registry", "x-feature": "identity", "x-renders": [], "description": "The platform's Dex client id: services.muster.clientId of the installation's config.yaml.patch, else the shared default's. The template renders it; the definition uses it for the managers' OAuth on the 3 line and the live probes."},
        "podCertificateRequest": {"type": "boolean", "x-source": "registry", "x-feature": "runtime", "x-renders": [], "description": "Whether the cluster serves certificates.k8s.io/v1beta1 PodCertificateRequest, which kagent's Agent Substrate needs on the 4 chart line: true when the cluster App's values on record (management-clusters/<name>/cluster-app-manifests.yaml) enable the feature gates PodCertificateRequest, ClusterTrustBundle and ClusterTrustBundleProjection under cluster.internal.advancedConfiguration for the API server, the controller manager and the kubelet, or when the cluster App's chart enables them by default (giantswarm/cluster 8.3.0 and later: cluster-aws 10.3.0, cluster-azure 9.3.0, cluster-cloud-director 7.3.0) — the chart at the version the App pins, or, for a release-based App that names its release (global.release.version) and no version, at the version the release lists for the chart in giantswarm/releases, read as the person; a release that cannot be read leaves the fact false with the reason on the report. Renders nothing: a 4-line record with kagent and false is refused at plan time naming the gates and the path; true renders the live probe that discovers the API and reads rolling until it is served."},
        "dexAppVersion": {"type": "string", "x-source": "registry", "x-feature": "identity", "x-renders": [], "description": "The dex-app the installation runs: spec.version of the App dex-app, from the installation's own pin — a patch on the App in management-clusters/<name>/collections/kustomization.yaml — where it has one, else from the fleet's shared base, bases/collections/shared/base/dex-app.yaml in the management-cluster-bases repository the kustomization's remote resource names, at the ref it names. Absent when neither could be read (the report carries why). Renders nothing: every Dex client renders as a referenced Secret, which dex-app takes from 3.2.0 and next to a hand-made inline secret from 3.2.2, so a version older than 3.2.2 holds the commit (commitRefused, naming the file to pin it in) while the comparison still runs; an absent version holds nothing."},
        "hub": {"type": "boolean", "default": false, "x-source": "registry", "x-feature": "federation", "x-renders": ["configmap:muster.muster.oauth.server.tokenExchangeBroker.targets.github", "configmap:muster.muster.oauth.server.tokenExchangeBroker.clientAudiences.<brokerClientId>[github]"], "description": "The hub installation: Giant Swarm's own managers run here and its broker releases the person's GitHub grant to the Dev Portal — the `github` grant target (grantIssuer https://github.com/login/oauth) among its broker client's audiences, which backs the portal's GitHub auth API without a GitHub App. The registry's hub, not every installation with targets: a customer aggregator brokers for its siblings but holds no GitHub grant."},
        "agentPlatform": {"type": "boolean", "default": false, "x-source": "registry", "x-feature": "runtime", "x-renders": ["record:agentPlatform.kagentApiV2"], "description": "Whether agent-platform is on record for the installation: its marker, installations/<name>/apps/agent-platform/configmap-values.yaml.patch, exists in its configs repository, whoever put it there. False is a fresh enable: where the record selects the 3 chart line and the fleet policy grants the organisation a component the 4 line alone carries (the cluster-manager), the enable selects the 4 line and writes agentPlatform.kagentApiV2: true into the record (installations/<name>/config.yaml.patch) as one more file of the configs pull request, keeping every other key. An installation on record keeps the line its record selects; a record on the 3 line granted such a component is refused as before."},
        "portals": {
          "type": "array",
          "x-source": "registry",
          "x-feature": "portal",
          "x-renders": ["dex-configmap:oidc.extraStaticClients[*] (backstage)", "dex-configmap:oidc.staticClients.dexK8SAuthenticator.trustedPeers", "configmap:muster.muster.oauth.server.trustedAudiences", "configmap:kagent.oauth2-proxy.extraArgs.oidc-extra-audience", "configmap:muster.muster.oauth.mcpClient.postLoginRedirectAllowlist", "configmap:gateway.jwksEgress", "configmap:extraObjects[*]", "configmap:agent-platform-mcps.agentgateway.jwt.extraProviders[*]", "backstage:app-config:*", "backstage:user-values:*", "backstage:kustomization:*"],
          "description": "The developer portals that sign people in on this installation: every portal whose gs.installations lists it, from the hub's app-config and the app-config of each customer-portal installation of the owning organisation. Each forwards the ID tokens of the Dex client it signs in through, whose id is an audience the platform trusts where the host's Dex patch carries it (clientId); the definition renders its own client for the portals (backstage, one redirect URI per portal), which every portal is trusted by as well. An id the installation trusts besides is not the definition's: the plan keeps it in the audience list it is on record in (the dex patch's trustedPeers, the platform patch's trustedAudiences, oidc-extra-audience or the edge provider's audiences), nowhere else. The platform's portal section is written into the installation's own portal, else into the organisation's portal on a sibling that is not hand-kept (a customer aggregator's); a hand-kept sibling portal, the hub's Dev Portal, carries its own section for the installations it proxies and takes no other installation's Component.",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["installation", "customer", "domain"],
            "properties": {
              "installation": {"type": "string", "minLength": 1, "description": "The installation whose management-clusters tree hosts the portal."},
              "customer": {"type": "string", "minLength": 1, "description": "The host's owning organisation; the platform's portal section is written into the installation's own portal, else the organisation's own portal, never another organisation's."},
              "domain": {"type": "string", "minLength": 1, "description": "The portal's hostname (app.baseUrl)."},
              "clientId": {"type": "string", "x-source": "registry", "description": "The id of the Dex client the portal signs in through, where the host's dex-app configmap patch (installations/<host>/apps/dex-app/configmap-values.yaml.patch) carries it: the oidc.extraStaticClients entry whose redirectURIs carry the portal's (https://<domain>/api/auth/oidc-<host>/handler/frame). The portal's app-config names the id only as a reference the chart resolves from the encrypted user-secrets. Absent when the patch carries no such client: the portal is then trusted through backstage, and an id the installation's own patches trust stays where it is on record."},
              "handKept": {"type": "boolean", "default": false, "x-source": "registry", "x-renders": ["backstage:app-config:app.extensions", "backstage:app-config:muster.installations"], "description": "The portal is hand-kept: its app-config on record (management-clusters/<host>/extras/backstage/backstage/app-config.yaml) carries a literal app.extensions list of its own where a portal the customer-portal definition renders includes the shared list ($include: shared-config.yaml#extensions). Backstage and helm-controller replace lists across config files and values sources, so on a hand-kept portal the platform's Component sets none of the portal's lists (app.extensions, muster.installations) and writes its object-shaped keys only (agentPlatform.kagent.installations.<name>, backstage.extraAppConfig), which merge; elsewhere the Component is the sole source of the two lists and sets them. The portal's environment, backstage.extraEnvVars, is the portal's own on every portal: the customer-portal definition's user values carry the whole list (the avatars image source, the tunnel's NODE_EXTRA_CA_CERTS) and the Component never sets it. The hub's Dev Portal reads true. Read, never typed: the day the customer-portal definition renders the portal the fact reads false and the Component carries the lists."},
              "grafanaWired": {"type": "boolean", "default": false, "x-source": "registry", "x-renders": ["backstage:app-config:app.extensions"], "description": "The portal's Grafana plugin is wired: its app-config on record carries the plugin's proxy endpoint (proxy.endpoints./grafana/api), the entry the customer-portal definition renders for plugins.grafana.enabled and reads the choice back from. The plugin's dashboards card is disabled in the app until a portal switches it on through its extension list, and Backstage keeps the Component's list whole over the portal's, so where the Component owns the lists its fragment includes the shared list with the platform's section and the card (shared-config.yaml#extensionsAgentPlatformGrafanaDashboards) instead of the one without the card (#extensionsAgentPlatform). Read, never typed: switching the plugin on in the customer-portal capability changes the fact, and the next agent-platform render follows it."},
              "chartLine": {"type": "string", "x-source": "registry", "description": "The chart line the portal follows: the ref its directory kustomization (management-clusters/<host>/extras/backstage/backstage/kustomization.yaml) patches onto the fleet base's backstage OCIRepository — the customer-portal definition's chart.line (>=A <B) or a tag. Before backstage 1.1.0 the portal's agent-platform plugin composes the agent's HelmRelease itself and reads the agents' Flux identity from agentPlatform.fluxServiceAccountName, which the portal section then names (kagent-flux); from 1.1.0 agents are created through agent-manager and the key is not read. Absent when the kustomization patches no ref: a plan with kagent on an installation whose hosted portal carries none, or one of another form, is refused naming the file."}
            }
          }
        },
        "federation": {
          "type": "object",
          "additionalProperties": false,
          "description": "The installation's place in the fleet's token exchange, from the portals' app-configs: the hubs whose broker exchanges tokens into it, and the targets its own muster brokers for.",
          "properties": {
            "hubs": {"type": "array", "items": {"type": "string", "minLength": 1}, "x-source": "registry", "x-feature": "federation", "x-renders": ["dex-configmap:oidc.extraStaticClients[*] (<hub>-token-exchange)", "dex-configmap:oidc.staticClients.dexK8SAuthenticator.trustedPeers", "extras:agent-platform/secrets/dex-client-<hub>-token-exchange.yaml"], "description": "Installations whose muster is the cluster-token broker of a portal that lists this one: each renders a token-exchange client in this installation's Dex."},
            "targets": {
              "type": "array",
              "x-source": "registry",
              "x-feature": "federation",
              "x-renders": ["configmap:muster.muster.oauth.server.tokenExchangeBroker", "configmap:agent-platform-mcps.identityProviders.<name>", "configmap:agent-platform-mcps.mcpServers", "configmap:muster.muster.extraCaFile", "extras:agent-platform/secrets/muster-broker-client-credentials.yaml", "extras:agent-platform/secrets/<name>-token-exchange-credentials.yaml", "extras:agent-platform/tunnelport/*", "teleport:values:tunnelport.tunnels[<app>-<name>]", "teleport:values:tunnelport.consumers.<hub>", "teleport:values:tunnelport.trustBundle.tokens[tunnelport-trust-bundle-token-<hub>]"],
              "description": "Installations this one brokers for: the portal whose cluster-token broker is this installation's muster lists them. Each target's own three MCP servers (kubernetes, prometheus, capi) join muster's list with exchange auth, the exchange at the target's Dex through the connector it registers for this hub (hubs); a private target is reached through the tunnel. On the hub (installation.hub) the broker also carries the `github` grant target.",
              "items": {
                "type": "object",
                "additionalProperties": false,
                "required": ["installation", "baseDomain", "private"],
                "properties": {
                  "installation": {"type": "string", "minLength": 1},
                  "baseDomain": {"type": "string", "minLength": 1},
                  "private": {"type": "boolean", "description": "The target's record: reached through Teleport, so this hub tunnels to it — a RemoteApp per tunnelled app on this hub and the app's tunnel entry among this hub's entries of teleport-fleet's tunnelport values; the tunnel's bots join by this hub's published service-account issuer (irsa.<base domain> on capa; another provider refuses a private target)."},
                  "platformProxied": {"type": "boolean", "default": false, "x-source": "registry", "x-feature": "federation", "x-renders": ["extras:agent-platform/tunnelport/remoteapps.yaml (kagent-<name>, agentgateway-<name>)", "teleport:values:tunnelport.tunnels[kagent-<name>]", "teleport:values:tunnelport.tunnels[agentgateway-<name>]"], "description": "The hub's portal proxies the target's agent platform: the app-config of a portal this installation brokers for lists the target under agentPlatform.kagent.installations, so the portal reaches the target's kagent (the UI and API v1 behind oauth2-proxy, probed at /ping) and its agentgateway (the kagent API v2 controller's gRPC listener) through the tunnel on this hub — a private target with it is tunnelled to both. A private target the portal reaches through the kubernetes tunnel alone gets neither."},
                  "hubs": {"type": "array", "default": [], "items": {"type": "string", "minLength": 1}, "x-source": "registry", "x-feature": "federation", "x-renders": ["configmap:muster.muster.oauth.server.tokenExchangeBroker.targets.<name>.connectorId", "configmap:agent-platform-mcps.identityProviders.<name>.connectorId"], "description": "The hubs of this installation's organisation that broker into the target, this installation among them: the cluster-token broker of every portal on record that lists the target, of the installations the registry gives the same owning organisation, the registry's hub first where it is one, then by name. The target's Dex registers one connector per hub, and only one of an organisation's hubs can carry the organisation's plain name (the policy's federation.connector.first, <customer>-simple-oidc): the first hub listed here; every further hub of the same organisation carries its own (federation.connector.further, <customer>-<hub>-oidc). Empty, this installation alone brokers into the target and carries the plain name. A list that does not name this installation is refused at plan time."}
                }
              }
            },
            "brokerClientId": {"type": "string", "x-source": "generated", "x-feature": "federation", "x-renders": ["configmap:muster.muster.oauth.server.tokenExchangeBroker.brokerClients.<id>", "configmap:muster.muster.oauth.server.tokenExchangeBroker.clientAudiences.<id>", "extras:agent-platform/secrets/muster-broker-client-credentials.yaml"], "description": "Dex client id of the hub's token-exchange broker client; registered once, read back from the hub's patch afterwards. Required on a hub."}
          }
        },
        "mcpServers": {
          "type": "array",
          "default": [],
          "x-source": "registry",
          "x-feature": "tool-access",
          "x-renders": ["configmap:muster.muster.oauth.mcpClient.tokenExchange.allowPrivateIP"],
          "description": "The MCP servers registered on this installation beyond the platform's own three: the MCPServer objects under management-clusters/<name>/extras/agent-platform/mcpservers/ that the directory's kustomization lists, where the tree's kustomization (extras/agent-platform/kustomization.yaml) lists the directory. A person registers a server by adding its MCPServer object there in muster's own shape — auth.forwardToken, auth.tokenExchange or auth.authorizationServer, the credentials Secret it references under ../secrets — and listing it; the platform renders nothing per server. It reads each one's state live, and where one exchanges tokens at a Dex of its own (auth.tokenExchange.dexTokenEndpoint) muster's token-exchange client is allowed to reach a private address: a workload cluster's Dex sits behind an internal load balancer, and the endpoint it is allowed for is on record. An entry of the values patch (agent-platform-mcps.mcpServers) is no registration: that list is the platform's own three and, on a hub, its targets'.",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["name", "url", "auth"],
            "properties": {
              "name": {"type": "string", "minLength": 1, "description": "The object's name in muster's namespace (agent-platform)."},
              "url": {"type": "string", "minLength": 1, "description": "spec.url: where muster reaches the server."},
              "auth": {"type": "string", "enum": ["none", "forward", "exchange", "oauth"], "description": "How muster authenticates to the server, in the agent-platform-mcps chart's words: none; forward, the person's own ID token (auth.forwardToken); exchange, a token exchanged at another Dex (auth.tokenExchange); oauth, a grant at the server's own authorization server (auth.authorizationServer)."},
              "dexTokenEndpoint": {"type": "string", "description": "For exchange: the token endpoint of the Dex the exchange runs at (auth.tokenExchange.dexTokenEndpoint)."}
            }
          }
        },
        "mcpClients": {
          "type": "array",
          "default": [],
          "x-source": "registry",
          "x-feature": "identity",
          "x-renders": ["configmap:muster.muster.oauth.server.trustedPublicRegistrationRedirectURIs"],
          "description": "The MCP clients registered on this installation with a stable HTTPS callback — a hosted agent runtime such as AWS Bedrock AgentCore, which registers itself with muster (dynamic client registration) and publishes no client metadata document: the ConfigMaps under management-clusters/<name>/extras/agent-platform/mcpclients/ that the directory's kustomization lists, where the tree's kustomization lists the directory, each one consumer with its redirect URIs (data.redirectURIs, one per line). muster registers such a client only for a redirect URI it trusts, and the definition renders that allowlist from these registrations, in the kustomization's order. A person registers a client by adding its ConfigMap there and listing it.",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["name", "redirectURIs"],
            "properties": {
              "name": {"type": "string", "minLength": 1, "description": "The ConfigMap's name: the consumer."},
              "redirectURIs": {"type": "array", "minItems": 1, "items": {"type": "string", "pattern": "^https://"}, "description": "The consumer's callback URIs, fully qualified HTTPS."}
            }
          }
        }
      }
    },
    "modelServing": {
      "type": "object",
      "additionalProperties": false,
      "description": "One choice: whether the installation serves models.",
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": false,
          "x-source": "person",
          "x-readback": {"file": "configmap", "key": "components.modelServing.enabled"},
          "x-feature": "runtime",
          "x-renders": ["configmap:components.kserve-llmisvc-crd.enabled", "configmap:components.kserve-llmisvc-resources.enabled", "configmap:components.kserve-runtime-configs.enabled", "configmap:components.modelServing.enabled", "configmap:modelServing.serving.runtimeClassName", "configmap:modelServing.modelsGateway.enabled"],
          "description": "Serve models on this installation: the llm-d control plane, the well-known LLMInferenceServiceConfigs, the model serving switch and the models Gateway (the meta chart's serving slice, 4.44.0 and later). The 4 chart line only; the GPU pool is the cluster-manager's."
        }
      }
    },
    "aiChat": {
      "type": "object",
      "additionalProperties": false,
      "description": "The other choice: the developer portal's AI chat, in the platform's portal section. The chat runs Claude on Anthropic's API (the key supplied at commit as aiChat.anthropic.apiKey) or on Vertex AI (aiChat.provider: vertex, with the Google project and location; the service account's JSON supplied at commit as aiChat.google.credentialsJson); a credential is never typed here or read back, and lands in the Component's own Secret agent-platform-ai-chat-credentials-backstage as the chart value the chart exposes as ANTHROPIC_API_KEY or mounts at /app/google/credentials.json. Read back from the Component's fragment on record, else from the portal's own app-config — a hand-kept portal is read from its app-config, and its chat moves into the Component at the next reconcile.",
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": false,
          "x-source": "person",
          "x-readback": {"file": ["backstage:agent-platform/app-config", "backstage:app-config"], "key": "aiChat", "kind": "present"},
          "x-feature": "portal",
          "x-renders": ["backstage:app-config:aiChat", "backstage:app-config:mcpActions", "backstage:app-config:backend.actions", "backstage:app-config:app.extensions.$include", "backstage:kustomization:resources[*]", "backstage:kustomization:patches[*]", "backstage:file:ai-chat-credentials.enc.yaml"],
          "description": "The portal's AI chat: the aiChat block on the provider with its two MCP servers (the portal's own actions server at /api/mcp-actions/v1 with the signed-in person's token, the installation's muster with the sign-in provider), the actions server's tool naming (mcpActions) and the actions the service lists for it (backend.actions), the credentials Secret appended to the portal HelmRelease's values sources, and on a rendered portal the shared extension list with the chat (shared-config.yaml#extensionsAgentPlatformAiChat, or its GrafanaDashboards pair where the portal's plugin is wired); a hand-kept portal keeps its own list, which carries the chat's entries already. Read back from the block's presence."
        },
        "model": {
          "type": "string",
          "default": "claude-opus-5",
          "x-source": "person",
          "x-readback": {"file": ["backstage:agent-platform/app-config", "backstage:app-config"], "key": "aiChat.model"},
          "x-feature": "portal",
          "x-renders": ["backstage:app-config:aiChat.model"],
          "description": "The model the chat answers with (aiChat.model), a Claude model id the provider serves. Read back from the block on record."
        },
        "provider": {
          "type": "string",
          "enum": ["anthropic", "vertex"],
          "default": "anthropic",
          "x-source": "person",
          "x-readback": {"file": ["backstage:agent-platform/app-config", "backstage:app-config"], "key": "aiChat.anthropic.provider"},
          "x-feature": "portal",
          "x-renders": ["backstage:app-config:aiChat.anthropic", "backstage:app-config:aiChat.google", "backstage:agent-platform/values:google", "backstage:file:ai-chat-credentials.enc.yaml"],
          "description": "Which platform serves the chat's Claude model: anthropic, Anthropic's own API with the key from the portal's environment (aiChat.anthropic.apiKey, supplied at commit); or vertex, Claude on Google Cloud Vertex AI in the project and region of aiChat.google, authenticated from the service account's JSON (aiChat.google.credentialsJson, supplied at commit) the chart mounts at /app/google/credentials.json — the block names the provider and the file, the Component's values carry the project and location the chart exports. Read back from aiChat.anthropic.provider on record; a block without it runs on Anthropic's API."
        },
        "google": {
          "type": "object",
          "additionalProperties": false,
          "description": "Where a chat on Vertex AI runs; both are named with aiChat.provider: vertex and refused empty there, unread otherwise.",
          "properties": {
            "project": {
              "type": "string",
              "x-source": "person",
              "x-readback": {"file": ["backstage:agent-platform/app-config", "backstage:app-config"], "key": "aiChat.google.project"},
              "x-feature": "portal",
              "x-renders": ["backstage:app-config:aiChat.google.project", "backstage:agent-platform/values:google.project"],
              "description": "The GCP project Vertex AI serves the model in (aiChat.google.project, the chart's GOOGLE_CLOUD_PROJECT). Read back from the block on record."
            },
            "location": {
              "type": "string",
              "x-source": "person",
              "x-readback": {"file": ["backstage:agent-platform/app-config", "backstage:app-config"], "key": "aiChat.google.location"},
              "x-feature": "portal",
              "x-renders": ["backstage:app-config:aiChat.google.location", "backstage:agent-platform/values:google.location"],
              "description": "The Vertex AI region, a multi-region such as eu for data residency or a single region (aiChat.google.location, the chart's GOOGLE_CLOUD_LOCATION). Read back from the block on record."
            }
          }
        }
      }
    }
  }
}
