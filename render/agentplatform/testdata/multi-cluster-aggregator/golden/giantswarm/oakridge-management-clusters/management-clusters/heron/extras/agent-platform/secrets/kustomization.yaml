# Rendered by giantswarm-platform-manager, agent-platform definition. Do not edit by hand:
# the next reconcile writes it again from the installation's inputs.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - muster-oauth-credentials.yaml
  - muster-valkey-credentials.yaml
  - dex-client-muster-secret.yaml
  - muster-broker-clients.yaml
  - kestrel-token-exchange-credentials.yaml
  - plover-token-exchange-credentials.yaml
