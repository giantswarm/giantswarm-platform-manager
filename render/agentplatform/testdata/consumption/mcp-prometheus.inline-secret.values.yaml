# The chart's own Secret: rendered once more without existingSecret and with
# dummy inline credentials, its keys are the contract an envFrom consumer reads
# by name. Applied over mcp-prometheus.values.yaml by the render-consumption test.
app:
  oauth:
    existingSecret: ""
    dexClientSecret: dummy
    encryptionKey: dummy
    storage:
      valkey:
        password: dummy
